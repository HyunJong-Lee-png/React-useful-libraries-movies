export interface DefaultThema {
  textColor: string
  bgColor: string
  btnColor: string
  accentColor: string
}

export const lightThema: DefaultThema = {
  textColor: 'black',
  bgColor: 'white',
  btnColor: 'tomato',
  accentColor: 'whitesmoke',
};

export const darkThema: DefaultThema = {
  textColor: 'white',
  bgColor: 'black',
  btnColor: 'tomato',
  accentColor: 'grey',
}